server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/elearn
    username: root
    password: 770320622
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.generator.infrastructure.persistence.po 

logging:
  level:
    com.changjiang.lowcode: DEBUG
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n" 